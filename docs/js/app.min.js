(function () {"use strict";var yearEl = document.getElementById("year");var yearText = String(new Date().getFullYear());if (yearEl) yearEl.textContent = yearText;document.querySelectorAll(".year-zh").forEach(function (el) {el.textContent = yearText;});var header = document.querySelector(".site-header");var toggle = document.querySelector(".nav__toggle");var menu = document.getElementById("nav-menu");var heroBg = document.querySelector(".hero-parallax__bg");var viewHome = document.getElementById("view-home");var viewBlog = document.getElementById("view-blog");var pageLoader = document.getElementById("page-loader");var loaderLogo = pageLoader? pageLoader.querySelector(".page-loader__logo"): null;var loaderBar = pageLoader? pageLoader.querySelector(".page-loader__bar"): null;var loaderTrack = pageLoader? pageLoader.querySelector(".page-loader__track"): null;var navClose = document.querySelector(".nav__close");var langToggle = document.getElementById("lang-toggle");var currentView = "home";var currentLang = "en";var LANG_STORAGE_KEY = "honbo-lang";var isViewLoading = false;var prefersReduced = window.matchMedia("(prefers-reduced-motion: reduce)").matches;var scrollAnimId = null;var isScrollAnimating = false;var revealObserver = null;var REVEAL_SELECTORS = ["#view-home .marquee-bar","#view-home .section-intro","#view-home .location-panel__body","#view-home .location-panel__media","#view-home .gallery-section .section-block__header","#view-home .bestsellers-section .section-block__header","#view-home .bestsellers-section__footnote","#view-home .origin .section-intro","#view-home .origin__body","#view-home .origin__note","#view-home .catering__title","#view-home .catering__subtitle","#view-home .catering__content > p","#view-home .catering__list","#view-home .catering__cta","#view-blog .blog-awards","#view-blog .blog-hero__title","#view-blog .blog-hero__lead","#view-blog .blog-hero__stats","#view-blog .review-card","#view-blog .blog-cta"];function getHeaderOffset() {if (!header) return 72;var h = header.offsetHeight;document.documentElement.style.setProperty("--header-h", h + "px");return h;}function getTargetTop(target) {return Math.max(0,target.getBoundingClientRect().top +window.pageYOffset -getHeaderOffset());}function easeInOutCubic(t) {return t < 0.5 ? 4 * t * t * t : 1 - Math.pow(-2 * t + 2, 3) / 2;}function updateHeader() {if (header) {header.classList.toggle("is-scrolled", window.scrollY > 60);}}function updateHeroParallax() {if (prefersReduced || !heroBg) return;var y = window.scrollY;var vh = window.innerHeight;var shift = y * 0.32;var scale = 1 + Math.min(y / vh, 1) * 0.12;heroBg.style.transform ="translate3d(0, " + shift + "px, 0) scale(" + scale + ")";}function runScrollEffects() {updateHeader();updateHeroParallax();}function onRevealIntersect(entries) {entries.forEach(function (entry) {if (!entry.isIntersecting) return;entry.target.classList.add("is-visible");if (revealObserver) revealObserver.unobserve(entry.target);});}function decorateRevealElement(el, index) {if (el.classList.contains("reveal")) return;el.classList.add("reveal");el.style.setProperty("--reveal-delay", (index % 5) * 0.08 + "s");if (el.classList.contains("location-panel__body")) {el.classList.add(el.closest(".location-panel--reverse")? "reveal--from-right": "reveal--from-left");}if (el.classList.contains("location-panel__media")) {el.classList.add(el.closest(".location-panel--reverse")? "reveal--from-left": "reveal--from-right");}if (el.classList.contains("review-card")) {el.classList.add("reveal--scale");el.style.setProperty("--reveal-delay",Array.prototype.indexOf.call(document.querySelectorAll("#view-blog .review-card"),el) *0.07 +"s");}}function refreshRevealsInContainer(container) {if (prefersReduced || !container) return;container.querySelectorAll(".reveal:not(.is-visible)").forEach(function (el) {var rect = el.getBoundingClientRect();if (rect.top < window.innerHeight * 0.92 && rect.bottom > 0) {el.classList.add("is-visible");if (revealObserver) revealObserver.unobserve(el);} else if (revealObserver) {revealObserver.observe(el);}});}function initRevealAnimations() {if (prefersReduced) return;if (!revealObserver) {revealObserver = new IntersectionObserver(onRevealIntersect, {threshold: 0.12,rootMargin: "0px 0px -8% 0px"});}REVEAL_SELECTORS.forEach(function (selector) {var nodes = document.querySelectorAll(selector);nodes.forEach(function (el, index) {decorateRevealElement(el, index);if (!el.classList.contains("is-visible")) {revealObserver.observe(el);}});});}function cancelScrollAnimation() {if (scrollAnimId !== null) {cancelAnimationFrame(scrollAnimId);scrollAnimId = null;}isScrollAnimating = false;document.body.classList.remove("is-scroll-animating");}function animatedScrollToY(targetY) {targetY = Math.max(0, targetY);var startY = window.pageYOffset;var distance = targetY - startY;if (Math.abs(distance) < 2) {runScrollEffects();return;}if (prefersReduced) {window.scrollTo(0, targetY);runScrollEffects();return;}cancelScrollAnimation();isScrollAnimating = true;document.body.classList.add("is-scroll-animating");var duration = Math.min(3200, Math.max(1000, Math.abs(distance) * 1.15));var startTime = null;function step(time) {if (!startTime) startTime = time;var elapsed = time - startTime;var progress = Math.min(elapsed / duration, 1);var eased = easeInOutCubic(progress);window.scrollTo(0, startY + distance * eased);runScrollEffects();if (progress < 1) {scrollAnimId = requestAnimationFrame(step);} else {window.scrollTo(0, targetY);runScrollEffects();cancelScrollAnimation();}}scrollAnimId = requestAnimationFrame(step);}function animatedScrollTo(target) {if (!target) return;animatedScrollToY(getTargetTop(target));}function scrollToTop() {animatedScrollToY(0);}function initAutoLoops() {document.querySelectorAll("[data-loop-speed]").forEach(function (track) {var inner = track.querySelector(".loop-track__inner");if (!inner) return;var seconds = parseFloat(track.getAttribute("data-loop-speed")) || 22;inner.style.animation = "loop-scroll " + seconds + "s linear infinite";});}function initHorizontalCarousels() {document.querySelectorAll(".horizontal-carousel").forEach(function (viewport) {var maxScroll = function () {return viewport.scrollWidth - viewport.clientWidth;};var drag = { active: false, startX: 0, startScroll: 0 };viewport.addEventListener("mousedown", function (e) {if (e.button !== 0 || maxScroll() <= 0) return;drag.active = true;drag.startX = e.pageX;drag.startScroll = viewport.scrollLeft;viewport.classList.add("is-dragging");});window.addEventListener("mousemove", function (e) {if (!drag.active) return;e.preventDefault();viewport.scrollLeft = drag.startScroll - (e.pageX - drag.startX);});window.addEventListener("mouseup", function () {if (!drag.active) return;drag.active = false;viewport.classList.remove("is-dragging");});});document.querySelectorAll(".horizontal-carousel--infinite").forEach(function (viewport) {var track = viewport.querySelector(".horizontal-carousel__track");var firstSet = track && track.querySelector(".loop-track__set");if (!track || !firstSet) return;track.querySelectorAll(".loop-track__set").forEach(function (set, index) {if (index > 0) set.remove();});var clone = firstSet.cloneNode(true);clone.setAttribute("aria-hidden", "true");clone.querySelectorAll("img[alt]").forEach(function (img) {img.setAttribute("alt", "");});track.appendChild(clone);var setWidth = 0;var looping = false;var wrapTimer = null;var WRAP_DELAY_MS = 220;function measure() {var sets = track.querySelectorAll(".loop-track__set");if (sets.length >= 2) {setWidth = sets[1].offsetLeft;} else {setWidth = firstSet.offsetWidth;}}function performWrap() {if (looping || setWidth < 1) return;var left = viewport.scrollLeft;if (left < setWidth - 8) return;looping = true;viewport.classList.add("is-looping");var next = left;while (next >= setWidth) {next -= setWidth;}viewport.scrollLeft = next;requestAnimationFrame(function () {requestAnimationFrame(function () {viewport.classList.remove("is-looping");looping = false;});});}function scheduleWrap() {clearTimeout(wrapTimer);wrapTimer = window.setTimeout(performWrap, WRAP_DELAY_MS);}function cancelWrap() {clearTimeout(wrapTimer);}measure();viewport.addEventListener("scroll", scheduleWrap, { passive: true });if ("onscrollend" in window) {viewport.addEventListener("scrollend", performWrap, { passive: true });}viewport.addEventListener("pointerdown", cancelWrap, { passive: true });viewport.addEventListener("pointerup", scheduleWrap, { passive: true });viewport.addEventListener("pointercancel", scheduleWrap, { passive: true });window.addEventListener("resize",function () {measure();},{ passive: true });window.addEventListener("load", measure);});}function setLoaderProgress(p) {var value = Math.max(0, Math.min(1, p));if (loaderLogo) {loaderLogo.style.setProperty("--load-p", String(value));}if (loaderBar) {loaderBar.style.width = value * 100 + "%";}if (loaderTrack) {loaderTrack.setAttribute("aria-valuenow", String(Math.round(value * 100)));}}function updateDocumentTitle() {if (currentView === "blog") {document.title =currentLang === "zh"? "媒體好評 | Honbo 漢堡": "Press & Reviews | Honbo 漢堡 Smash Burgers Hong Kong";return;}document.title =currentLang === "zh"? "Honbo 漢堡 | 香港壓扁漢堡 — 灣仔及中環": "Honbo 漢堡 | Smash Burgers Hong Kong — Wan Chai & Central";}function updateLangToggle() {if (!langToggle) return;langToggle.textContent = currentLang === "en" ? "中文" : "EN";langToggle.setAttribute("aria-label",currentLang === "en" ? "Switch to Chinese" : "Switch to English");}function applyLanguage(lang, skipTransition) {if (lang !== "en" && lang !== "zh") return;if (lang === currentLang) return;function swap() {currentLang = lang;document.documentElement.lang = lang === "zh" ? "zh-HK" : "en-HK";document.body.classList.remove("is-lang-en", "is-lang-zh");if (lang === "zh") {document.body.classList.add("is-lang-zh");}updateLangToggle();updateDocumentTitle();closeMobileMenu();try {localStorage.setItem(LANG_STORAGE_KEY, lang);} catch (err) {}document.body.classList.remove("is-lang-switching");}if (skipTransition || prefersReduced) {swap();return;}document.body.classList.add("is-lang-switching");window.setTimeout(swap, 160);}function initLanguage() {var stored = null;try {stored = localStorage.getItem(LANG_STORAGE_KEY);} catch (err) {stored = null;}if (stored === "zh" || stored === "en") {applyLanguage(stored, true);return;}document.body.classList.remove("is-lang-zh");updateLangToggle();updateDocumentTitle();}function runPageLoader(onDone) {if (!pageLoader || prefersReduced) {if (onDone) onDone();return;}pageLoader.hidden = false;pageLoader.setAttribute("aria-hidden", "false");setLoaderProgress(0);var duration = 1200;var start = performance.now();function frame(now) {var elapsed = now - start;var progress = Math.min(elapsed / duration, 1);setLoaderProgress(easeInOutCubic(progress));if (progress < 1) {requestAnimationFrame(frame);} else {window.setTimeout(function () {pageLoader.hidden = true;pageLoader.setAttribute("aria-hidden", "true");if (onDone) onDone();}, 120);}}requestAnimationFrame(frame);}function applyView(view, skipLoader) {if (!viewHome || !viewBlog) return;if (view !== "home" && view !== "blog") return;if (view === currentView) return;function swap() {currentView = view;viewHome.hidden = view !== "home";viewBlog.hidden = view !== "blog";document.body.classList.toggle("is-blog-view", view === "blog");if (view === "blog") {window.scrollTo(0, 0);if (header) header.classList.remove("is-scrolled");} else {runScrollEffects();}updateDocumentTitle();if (history.replaceState) {history.replaceState(null,"",view === "blog" ? "#press" : window.location.pathname + window.location.search);} else {window.location.hash = view === "blog" ? "press" : "";}isViewLoading = false;refreshRevealsInContainer(view === "blog" ? viewBlog : viewHome);}if (skipLoader || prefersReduced) {swap();return;}isViewLoading = true;runPageLoader(swap);}function closeMobileMenu() {if (!toggle || !menu) return;toggle.setAttribute("aria-expanded", "false");menu.classList.remove("is-open");document.body.classList.remove("is-menu-open");}function openMobileMenu() {if (!toggle || !menu) return;toggle.setAttribute("aria-expanded", "true");menu.classList.add("is-open");document.body.classList.add("is-menu-open");}function bindNavScroll() {document.querySelectorAll('a[href^="#"]').forEach(function (link) {link.addEventListener("click", function (e) {var hash = link.getAttribute("href");if (!hash || hash === "#") return;var viewTarget = link.getAttribute("data-view");var delay =menu && menu.classList.contains("is-open") && window.innerWidth <= 768? 260: 0;if (viewTarget === "blog" || hash === "#press" || hash === "#blog") {e.preventDefault();closeMobileMenu();window.setTimeout(function () {applyView("blog");}, delay);return;}if (viewTarget === "home" && currentView === "blog") {e.preventDefault();closeMobileMenu();window.setTimeout(function () {applyView("home", false);window.setTimeout(function () {if (hash === "#top") {scrollToTop();return;}var target = document.getElementById(hash.slice(1));if (target) animatedScrollTo(target);}, 1300);}, delay);return;}if (currentView !== "home") return;if (hash === "#top") {e.preventDefault();closeMobileMenu();window.setTimeout(function () {scrollToTop();}, delay);return;}var target = document.getElementById(hash.slice(1));if (!target) return;e.preventDefault();closeMobileMenu();window.setTimeout(function () {animatedScrollTo(target);}, delay);});});}function initViewFromHash() {if (window.location.hash === "#press" || window.location.hash === "#blog") {applyView("blog", true);}}function onScroll() {if (isScrollAnimating) return;runScrollEffects();}if (toggle && menu) {toggle.addEventListener("click", function (e) {e.stopPropagation();if (toggle.getAttribute("aria-expanded") === "true") {closeMobileMenu();} else {openMobileMenu();}});document.addEventListener("click", function (e) {if (!menu.classList.contains("is-open")) return;if (menu.contains(e.target) || toggle.contains(e.target)) return;closeMobileMenu();});}if (navClose) {navClose.addEventListener("click", function (e) {e.stopPropagation();closeMobileMenu();});}window.addEventListener("hashchange", function () {if (isViewLoading) return;if ((window.location.hash === "#press" || window.location.hash === "#blog") &&currentView !== "blog") {applyView("blog", true);} else if (window.location.hash !== "#press" &&window.location.hash !== "#blog" &&currentView === "blog") {applyView("home", true);}});function showCopyToast(message) {var toast = document.getElementById("copy-toast");if (!toast) return;toast.textContent = message;toast.classList.remove("is-visible");window.clearTimeout(showCopyToast._hideTimer);window.clearTimeout(showCopyToast._resetTimer);window.requestAnimationFrame(function () {window.requestAnimationFrame(function () {toast.classList.add("is-visible");});});showCopyToast._hideTimer = window.setTimeout(function () {toast.classList.remove("is-visible");showCopyToast._resetTimer = window.setTimeout(function () {toast.textContent = "Phone number copied · 電話號碼已複製";}, 350);}, 2400);}function copyTextSync(text) {var area = document.createElement("textarea");area.value = text;area.setAttribute("readonly", "");area.style.position = "fixed";area.style.top = "0";area.style.left = "0";area.style.width = "1px";area.style.height = "1px";area.style.opacity = "0";area.style.pointerEvents = "none";document.body.appendChild(area);area.focus();area.select();area.setSelectionRange(0, text.length);var copied = false;try {copied = document.execCommand("copy");} catch (err) {copied = false;}document.body.removeChild(area);return copied;}function initCateringPhoneCopy() {var button = document.querySelector(".catering__phone-copy");if (!button) return;button.addEventListener("click", function () {var text = button.getAttribute("data-copy-text") || "";if (!text) return;var successMsg =currentLang === "zh"? button.getAttribute("data-copy-toast-zh") || "電話號碼已複製": button.getAttribute("data-copy-toast-en") || "Phone number copied";var failMsg =currentLang === "zh"? button.getAttribute("data-copy-fail-zh") || "未能複製電話": button.getAttribute("data-copy-fail-en") || "Could not copy number";if (copyTextSync(text)) {showCopyToast(successMsg);return;}if (navigator.clipboard && navigator.clipboard.writeText) {navigator.clipboard.writeText(text).then(function () {showCopyToast(successMsg);}).catch(function () {showCopyToast(failMsg);});return;}showCopyToast(failMsg);});}if (langToggle) {langToggle.addEventListener("click", function () {applyLanguage(currentLang === "en" ? "zh" : "en");});}initLanguage();bindNavScroll();initViewFromHash();initCateringPhoneCopy();initRevealAnimations();initAutoLoops();initHorizontalCarousels();getHeaderOffset();window.addEventListener("scroll", onScroll, { passive: true });window.addEventListener("resize", getHeaderOffset, { passive: true });window.addEventListener("load", runScrollEffects);runScrollEffects();})();